import 'package:flutter/material.dart';

class WeightageAddpage extends StatelessWidget {
  const WeightageAddpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}